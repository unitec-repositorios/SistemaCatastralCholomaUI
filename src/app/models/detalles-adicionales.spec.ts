import { DetallesAdicionales } from './detalles-adicionales';

describe('DetallesAdicionales', () => {
  it('should create an instance', () => {
    expect(new DetallesAdicionales()).toBeTruthy();
  });
});
