import { CaracterisiticasPropiedad } from './caracterisiticas-propiedad';

describe('CaracterisiticasPropiedad', () => {
  it('should create an instance', () => {
    expect(new CaracterisiticasPropiedad()).toBeTruthy();
  });
});
