import { DatosComplementarios } from './datos-complementarios';

describe('DatosComplementarios', () => {
  it('should create an instance', () => {
    expect(new DatosComplementarios()).toBeTruthy();
  });
});
